import { createGlobalStyle } from 'styled-components';

/*
 * theme palette overrides can go here sparingly if needed
 * TODO: work .less vars into build instead
 */

const Theme = createGlobalStyle`
`;

export default Theme;
